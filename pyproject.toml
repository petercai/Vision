[project]
name = "uml-gen"
version = "1.0.0"
description = "Generate UML diagrams from source code."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pyyaml>=6.0.3",
    "tree-sitter>=0.25.2",
    "tree-sitter-java>=0.23.5",
]
