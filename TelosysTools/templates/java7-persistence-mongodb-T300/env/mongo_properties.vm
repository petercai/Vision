${SHARP} ---
${SHARP} Created on $today.date ( Time $today.time )
${SHARP} Generated by $generator.name ( version $generator.version )
${SHARP} ---

${SHARP} MONGO default configuration

${SHARP} Mongo database name
mongo.dbname=mydb

${SHARP} Mongo hostname 
mongo.hostname=localhost

${SHARP} Mongo port (default is 27017)
${SHARP} mongo.port=27017

${SHARP} User password
mongo.password=mypass

${SHARP} User username
mongo.username=me
